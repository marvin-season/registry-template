---
title: Use Intersection Observer
description: A hook to check if an element is intersecting with the viewport
---

import { useIntersectionObserver } from "./index";
import { Demo } from "./demo";

## Use Intersection Observer

The `useIntersectionObserver` hook is a hook that checks if an element is intersecting with the viewport.

## Installation

<InstallCommand pkgName="use-intersection-observer" />

## Preview

<PreviewCode pkgName="use-intersection-observer">
  <Demo />
</PreviewCode>
